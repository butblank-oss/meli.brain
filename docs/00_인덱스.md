# 맬리브레인 3기 종료 리포트 — 개발 핸드오프 최종판

**v3.1 · 2026-09-07** · 인천남동구 보건소 · 2026 맬리브레인 3기 종료 리포트

---

## 1. 정본 규칙

이 판의 정본은 **`index.html` (검수용 · v3.1 · 2026-09-07)** 하나입니다. 왼쪽 패널의 `📋 변경이력` 버튼으로 이력을 바로 볼 수 있습니다.

- 문구 · 값 · 레이아웃 · 예외 동작이 문서와 어긋나면 **검수용 HTML을 따릅니다**.
- 피그마 아트보드는 시각 검수용 참고이며, 아직 남은 차이는 문서 3 §15 부록 A에 정리했습니다.
- 이전 판(핸드오프 v1.0 · 화면설계서 v2 · 예외 상태 명세 v1.0 · IA v1.0 · 구 프로토타입 .dc)은 **모두 이 폴더의 문서로 대체**됐고, 원본 파일은 `../구버전_20260901/`로 옮겼습니다.

---

## 2. 문서 구성

| # | 문서 | 다루는 것 | 언제 보나 |
|---|---|---|---|
| 1 | 정책 · 상수 · 산식 | K-01~K-14 상수 · F-01~F-16 산식 · 13단계 환산 매핑 · 수료 분기 · 문구 정책 · 게이지 각도 | 값을 계산해야 할 때 |
| 2 | 예외 상태 명세 | P01~P19 · 화면별 예외 케이스 65개 · 게임 점수 결손 · 검수 방법 | 데이터가 없거나 부족할 때 |
| 3 | 화면설계서 | 화면 9장 · 요소 표 전량(S1 ~ S8) · 공통 레이아웃 · 모션 M-01~M-11 · API 35필드 · 검수 체크리스트 | 화면을 그릴 때 |
| 4 | IA · 개발 티켓 | 오버레이 구조 · 진입 플로우 F1~F3 · 규칙 IA-R-01~12 · 홈 배너 · 티켓 27개 · 구현 순서 | 무엇을 어떤 순서로 만들지 |

각 문서는 `.md`(원본)와 `.html`(읽기용) 두 벌로 있으며 내용은 동일합니다. `build_docs.py`가 `.md`를 읽어 `.html`을 생성하므로 **수정은 항상 `.md`에서** 하고 다시 실행하세요.

```bash
python3 build_docs.py
```

---

## 3. 30초 요약

- 리포트는 **홈 위 전체화면 오버레이**이고, 그 안에 **8개 섹션이 이어진 단일 스크롤 문서**입니다. 닫기(X)는 뷰포트 fixed로 **전 구간에 하나**.
- 기간은 **2026-03-03 ~ 10-31**(표기), 참여율 분모는 **242일**(3/3~10/30), 모수는 **823명**.
- 수료 분기는 **참여일 30일 OR 활동 30건**이며 서버 `cert.tier`를 그대로 신뢰합니다.
- **랭킹은 활동 점수 기준**입니다. 게임 점수는 8/18 이전이 영구 결손이라 화면 어디에도 쓰지 않습니다.
- **모든 섹션은 항상 노출**합니다. 명시적 예외는 **걸음 미연동(P16)** 하나뿐이고, 그 경우 리포트가 7개 섹션이 됩니다.
- **0과 "–"는 다릅니다.** P02는 언제나 0, P03은 언제나 "–"(en dash).
- 마음 게이지는 **정확한 180° 반원**이고 `각도 = 180 − 점수 × 12`입니다.
- **스크롤 리빌은 progressive enhancement**입니다. 기본 상태가 완성된 리포트여야 하며, 그렇지 않으면 증서 캡처가 빈 화면으로 나옵니다.
- SC-RP-07의 "4기도 참여하기"가 **이 리포트의 유일한 전환 지점**이고, 바텀시트 확인 후 SC-RP-08 결과 화면으로 이어집니다.

---

## 4. 이번 판에서 바뀐 것 (요약)

| 영역 | 변경 |
|---|---|
| 게이지 | 125° 스윕 → **180° 반원** (`180 − 점수 × 12`) |
| 환산 매핑 | 거리 · 칼로리 각 8단계 → **각 13단계** (500kcal 이상 음식 · 100~400km 도시 추가) |
| 랭킹 | 그룹 블러 3px → **행 단위 blur 5px + 투명도 50%**, 내 행만 선명 |
| 랭킹 | 구분선 구간 문구 삭제 · 상위 % 배지에 코드 부여(S4B-06) |
| 타일 폭 | 04B **160 : 160** · 06 **180 : 140** (7자리 걸음 대응) |
| 06 | 요소 12개로 세분화 · 음식 아이콘 13종 적용 · S6 코드 체계로 정리 |
| 07 | 영문 kicker 삭제 · 배지 "맬리브레인 3기" 고정 · 헤딩 분기 3종 · 증서 본문 3줄 14px |
| 신규 | **SC-RP-08 4기 신청 완료 / 실패** · 07-h 바텀시트 · 07-l 완료 후 비활성 상태 |
| 문구 | 자리표시 문구 **"게임 기록이 없어요"**로 통일 · S4-04 라벨 "이 게임을 한 횟수" |
| 정합 | P06 요일 동률 → **"골고루"** 확정 · 원칙 ①의 유일한 예외가 P16임을 두 문서에 동일하게 기술 |

상세 근거는 문서 1 §9에 표로 있습니다. **2026-09-07 개발팀 확인 요청 19건에 대한 회신은 문서 5**에 있으며, 그 결정(증서 실명 · 마스킹 규칙 · F-11 반올림 · 8/1 경계 · 강제 업데이트 · 배너 유지 등)은 문서 1~4에 반영돼 있습니다.

---

## 5. 지금 막혀 있는 것

개발 착수 전에 닫혀야 하는 항목만 추렸습니다. 전체 목록은 문서 1 §10과 문서 4 §8에 있습니다.

| 코드 | 항목 | 회신 |
|---|---|---|
| Q-11 | 3D 캐릭터 2종 · 게임 썸네일 16종 (음식 아이콘 13종은 수령 완료) | 디자인 |
| Q-13 | 4기 신청 안내 **문의 전화번호** (K-14 · 현재 `{전화번호}` 자리표시) | 기획팀 |
| Q-02 | B분기 저장 버튼 라벨 (현재 가정 "참여기록 저장하기") | 기획팀 |
| IA-Q-04 | 11/2 강제 업데이트 · 리포트 공개 사전 안내 문구 (푸시 · 공지) | 기획팀 |
| D-32 | F-15 활동 점수의 소스 테이블 · 컬럼 | 개발팀 |
| D-33 | F-16 "3개 프로그램"의 구체적 지정 | 기획팀 → 개발팀 |
| IA-Q-07 | 4기 신청 API 엔드포인트 · 실패 코드 정의 | 개발팀 |
| Q-06 | 보건소 10/30 재검사 입력 완료 예상 시각 (K-05 확정용) | 기획팀 → 개발팀 |

---

## 6. 파일 목록

| 파일 | 내용 |
|---|---|
| `00_인덱스.md` / `.html` | 이 문서 |
| `01_정책·상수·산식.md` / `.html` | 문서 1 |
| `02_예외상태 명세.md` / `.html` | 문서 2 |
| `03_화면설계서.md` / `.html` | 문서 3 |
| `04_IA·개발티켓.md` / `.html` | 문서 4 |
| `05_개발팀 확인요청 회신_20260907.md` / `.html` | 문서 5 · 개발팀 질문 19건 회신 (내부용) |
| `06_기획회신-20260907.md` / `.html` | 문서 6 · 개발팀 회신용 (7컬럼) |
| `07_변경이력.md` / `.html` | 문서 7 · 변경이력 (index.html 변경이력 패널과 동일) |
| `08_클로드디자인 프롬프트_20260907.md` | 클로드디자인 프로토타입 반영 프롬프트 |
| `build_docs.py` | `.md` → `.html` 변환 스크립트 |
| `../index.html` | **정본 v3.1** · 전 화면 · 예외 65개 · 전 모션 · 변경이력 패널 |

---

v3.1 · 2026-09-07
