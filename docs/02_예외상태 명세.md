# 맬리브레인 3기 종료 리포트 — 예외 상태 명세

문서 2 / 4 · **최종판 v3.1 · 2026-09-07** (v3.0 2026-09-02 기반) · 모수 3기 챌린저 실사용자 823명 (2026-08-24 becare prod 조회)

> 8개 화면 각각에서 데이터가 없거나 부족할 때 무엇을 보여줄지 정의합니다. 이 판의 정본은 `index.html` (검수용 · **v3.1 · 2026-09-07**)이며, 아래 예외 케이스 코드는 그 HTML의 검수 패널 선택지와 **1:1로 대응**합니다. 산식 · 상수는 문서 1(정책 · 상수 · 산식), 요소 스펙은 문서 3(화면설계서)을 따릅니다.

---

## 1. 공통 원칙

1. **모든 화면은 항상 노출한다** — 데이터가 없다는 이유로 섹션을 건너뛰지 않습니다. **명시적 예외는 걸음 미연동(P16) 하나뿐**입니다. 해당 사용자(27명 · 3.3%)의 리포트는 7개 섹션이 됩니다. 연동은 됐지만 걸은 날이 0일인 경우(P17)는 이 예외에 해당하지 않습니다 — 0이 실제 측정 결과이므로 섹션을 그대로 노출합니다.
2. **수치가 없으면 0으로 표기한다** — 빈칸이나 하이픈이 아닌 0입니다 (P02).
3. **집계 자체가 불가한 경우에만 "–"를 쓴다** — en dash U+2013, 단위와 붙여 씁니다 (P03).
4. **값이 하나뿐이면 그것이 현재 상태다** — 비교 대상이 없다고 값을 숨기지 않습니다 (P04).

> ! 0과 "–"의 구분이 이 문서에서 가장 자주 틀리는 지점입니다. **P02는 언제나 0, P03은 언제나 "–"**이며 한 코드가 상황에 따라 둘을 오가지 않습니다. 걸음 미연동은 "알 수 없다"라 원칙상 "–"지만 섹션 전체를 감추는 쪽으로 확정했고, 그래서 P03에 섞지 않고 P16으로 뺐습니다.

### 카드는 감출 수 있고, 섹션은 감출 수 없습니다

원칙 ①은 **섹션(화면) 단위**에 적용됩니다. 카드 단위로 감추는 경우는 네 군데입니다.

- 게임 0종일 때 — 하이라이트 다크 카드(S4-03)
- 총 걸음 0 또는 7km 미만일 때 — 거리 환산 카드(S6-07 ~ S6-09)
- 총 걸음 0일 때 — 칼로리 환산 카드(S6-10 · S6-11)
- 최하위일 때 — 하단 "아래로 N명" 요약(S4B-08)

"0km를 걸었어요"나 "약 0kcal을 태웠어요"는 격려가 아니라 조롱으로 읽히기 때문입니다. 그 외에는 카드도 0을 담은 채로 남깁니다.

### 한 화면은 812px을 넘지 않습니다

기준 단말은 안드로이드이고 아트보드는 375 × 812입니다. 콘텐츠가 넘치면 카드를 줄이는 대신 **화면을 나눕니다** — 이 원칙으로 SC-RP-04가 04와 04B로 갈라졌습니다. 다만 아트보드가 8~9장으로 보일 뿐, 실제 리포트는 **오버레이 하나 안에 8개 섹션이 이어진 단일 스크롤 문서**입니다. 그래서 닫기(X)는 뷰포트 fixed로 전 구간에 **하나만** 떠 있고, 스크롤 힌트는 섹션마다 하나씩 있어 스크롤과 함께 지나갑니다.

---

## 2. 공통 예외 처리 코드 (P코드)

| 코드 | 상태 | 표시 규칙 | 적용 화면 |
|---|---|---|---|
| P01 | 정상 | 값이 모두 존재. 정의된 서식 그대로 표기 | 전 화면 |
| P02 | 값이 0 | 숫자 0으로 표기. 카드 · 섹션 유지, 숨기지 않음. **카운트업 애니메이션은 생략** (0→0을 세는 모션은 의미가 없음) | 참여일 · 게임 횟수 · 마음 기록 · 걸음수 |
| P03 | 집계 불가 | **"–"(en dash U+2013) 표기.** 하이픈(-)이 아니며 단위와 붙여 씁니다 — "–점". 집계는 됐고 값이 0인 경우(P02)와 구분됩니다. 이 코드는 언제나 "–"만 지시하며 0을 지시하지 않습니다. 데이터가 없다는 이유로 섹션을 감추지도 않습니다 | 뇌 점수 미측정 · 대표 요일 · 시간 없음 |
| P04 | 측정 1회만 | 그 값이 곧 현재 상태입니다. **화면마다 처리가 다릅니다.** **SC-RP-02** — 있는 값과 화살표는 그대로 두고 **없는 쪽만 "–점"**으로 채웁니다. 그래프는 단일 포인트 + 수평 점선. **SC-RP-05** — 실선 바늘만 두고 **고스트는 감춥니다**. 10/30 재검사 입력 전까지 전원이 이 상태이므로 사실상 기본 화면입니다 | SC-RP-02 뇌 점수 · SC-RP-05 게이지 |
| P05 | 항목 수 부족 | 리스트는 있는 만큼만 행을 만들고 빈 행을 두지 않습니다. 0개면 카드를 감추고 한 줄 안내로 대체합니다. **단 SC-RP-04 TOP 3만 예외로 3행을 고정**합니다 — 카드 제목이 "TOP 3"을 약속하기 때문입니다. 모자란 행은 순위 숫자를 남기고 썸네일 투명도 35%, 게임명 자리에 **"게임 기록이 없어요"**(#9AA3AD), 횟수는 감춥니다 | SC-RP-04 TOP 3 · SC-RP-04B 전체 랭킹 |
| P06 | 동점 | **요일 동률 → 타일은 "골고루", 해당 막대는 모두 강조**합니다. 동률 요일이 2개든 5개든 표기는 같으며, 몇 개가 동률인지는 막대가 답합니다. 개인화 멘트에서는 요일을 빼고 시간만 씁니다. **시간 동점은 이른 시간**을 고릅니다 — 막대가 없어 "골고루"의 근거를 화면에서 보여줄 수 없기 때문입니다. **게임 동점은 노션 정렬 앞** | 대표 요일 · 대표 시간 · 최다 게임 |
| P07 | 상한 초과 | **중앙 숫자는 실제 달성률을 그대로 표기**합니다(예: 124%). 값을 100%로 깎지 않습니다. **아크만 한 바퀴에서 멈춥니다.** 별도 병기 문구를 두지 않으며 각주는 "하루 목표 10,000보 기준" 그대로입니다 | SC-RP-06 목표 달성률 · 거리 진행바 |
| P08 | 최하위 · 단독 | 공동 최하위도 순위를 그대로 표기("823명 중 823위"). 순위를 감추지 않음. 단, 하단 "아래로 N명" 문구는 0명이 되므로 감춤 | SC-RP-04B 랭킹 |
| P09 | 배치 미완료 | payload 없음 · `generated_at` 부재 → 리포트 진입 차단하고 "리포트를 준비하고 있어요" 안내. **부분 렌더 금지** | 전 화면 · 진입 게이트 |
| P10 | 탈퇴자 | 리포트 발급 대상 제외. 진입 시 안내 후 홈 복귀. 배너도 미노출 | 전 화면 |
| P11 | 아이콘 로딩 실패 | 그레이 원형 placeholder 표시. 레이아웃 밀림 없이 동일 크기 유지 | 캐릭터 2종 · 음식 아이콘 13종 · 게임 썸네일 |
| P12 | 표기명 · 마스킹 | **표기명 = 닉네임 → 이름 → "회원님"** 순. 마스킹은 **첫 · 끝 글자만 남기고 가운데 전부 `*`** — 3자 김맬리 → 김*리, 4자 남궁민수 → 남**수, 2자 김맬 → 김*, 1자 그대로. 글자 수는 공백 포함 문자 수로 세고 문자 종류와 무관하게 같은 규칙. 커버 · 랭킹(타인 포함)에 적용. **증서(S7-06)만 예외 — 원문 그대로** (2026-09-07 확정). 마스킹은 서버에서 처리하며 payload는 `masked_name`(화면용)과 `cert_name`(증서용 원문)을 따로 담음 | SC-RP-01 · 04B · 07 |
| P13 | 문구 케이스 분기 | 데이터 상태에 따라 문구만 교체하고 **레이아웃은 변경하지 않습니다**. SC-RP-05 안내박스는 **종료 구간(K-12) 하나만** 보고 고릅니다 — 문구 4쌍. **변화 방향은 문구에 반영하지 않습니다.** 헤드라인은 S5-06 상태 텍스트와 **항상 같은 구간**을 가리켜야 합니다 | 개인화 멘트 전체 · 수료 분기 |
| P14 | 재검사 미입력 | 종료점 미산출 → P04로 처리. 배치 재실행(K-05) 후 자동 산출 · 노출 | SC-RP-02 · SC-RP-05 |
| P15 | 캡처 실패 | 증서 저장 실패 시 재시도 1회 후 "저장하지 못했습니다" 안내 카드. 캡처 대상은 blur 없는 불투명 컨테이너로 유지 | SC-RP-07 저장하기 |
| P16 | 걸음 미연동 | **SC-RP-06 섹션 전체를 렌더하지 않습니다** (2026-08-31 확정). 해당 사용자의 리포트는 **7개 섹션**이 됩니다. payload는 `connected: false` + 값 0으로 그대로 내려오며, 클라이언트가 `connected`로 분기해 섹션을 건너뜁니다 | SC-RP-06 전체 · 3기 27명(3.3%) |
| P17 | 걸은 날 0일 | 연동은 됐지만 기간 내 걸음 기록이 한 건도 없는 경우. **하루 평균의 분모가 0이 되므로 0으로 나누지 않고 하루 평균을 0으로 정의**합니다. **P16과 달리 섹션을 노출**합니다. 총 걸음 0이므로 거리 · 칼로리 카드만 감춥니다 | SC-RP-06 · 섹션 유지 |
| P18 | 네트워크 오류 | payload 요청 실패 · 타임아웃. **전체 화면 안내로 전환**하고 [다시 시도] 버튼을 둡니다. 부분 렌더나 빈 화면을 남기지 않습니다. 자동 재시도는 1회까지. **P09는 서버에 값이 없는 것이고 P18은 값을 못 받아온 것**이라 안내 문구와 복구 방법이 다릅니다 | 전 화면 · 진입 게이트 |
| P19 | 로딩 | 오버레이 진입 후 payload 도착 전. 커버 배경 위에 **스피너 + "리포트를 불러오는 중이에요"**. 3초를 넘기면 "조금만 기다려주세요" 한 줄을 덧붙입니다. **스켈레톤을 쓰지 않습니다** — 8개 섹션의 형태가 제각각이라 스켈레톤이 실제 화면을 예고하지 못하고 깜빡임만 늘립니다 | 전 화면 · 진입 게이트 |

### 최소 데이터 대표값 규칙 (P06 보충)

데이터가 아주 적어도 대표값은 뽑습니다. 화 · 목에만 참여했고 화가 1회 더 많으면 "화요일"이 대표 요일입니다. 데이터가 1건뿐이면 그 1건이 대표값입니다. **동점일 때만** 아래를 적용합니다.

- **요일 동률 → "골고루"**, 동률 막대는 모두 강조 (개인화 멘트에서는 요일을 빼고 시간만 씀)
- **시간 동점 → 이른 시간** (0시 > 1시 > … · 막대가 없어 "골고루"의 근거를 보여줄 수 없음)
- **게임 동점 → 노션 DB 정렬 앞**

---

## 3. 화면별 예외 케이스 목록

케이스 코드는 검수용 HTML의 좌측 패널 선택지와 같습니다. 각 화면의 **정상**은 별도 코드 없이 `0`으로 표기합니다.

### SC-RP-01 커버

| 케이스 | 조건 | 화면 처리 | P코드 |
|---|---|---|---|
| 0 | 정상 | "김*리 님," · "242일 중 **142일**의 기록이 담겨 있어요" | P01 |
| 01-a | 참여일 0일 | **"242일 중 0일"** 그대로 표기. 문구 변경 · 숨김 없음 | P02 |
| 01-b | 이름 미설정 | 헤딩을 **"회원님,"**으로. "회원님 님" 금지 | P12 |
| 01-c | 0일 + 이름 미설정 | 두 처리를 동시에 적용 | P02 · P12 |
| 01-d | 4자 이름 | 마스킹 규칙 검증 — 남궁민수 → **"남**수 님,"** (첫·끝만 남김) | P12 |

히어로 캐릭터 영역은 데이터와 무관하게 항상 표시합니다.

### SC-RP-02 숫자 요약

| 케이스 | 조건 | 화면 처리 | P코드 |
|---|---|---|---|
| 0 | 측정 2회 · 상승 | `69점 → 77점` · 꺾은선 상승 | P01 |
| 02-a | 시작만 있음 | 시작값 + 화살표는 그대로, **종료만 "–점"**. 그래프는 좌측 단일 포인트 + 수평 점선 | P04 |
| 02-b | 종료만 있음 | 시작만 "–점". 그래프는 **우측** 단일 포인트 + 수평 점선 | P04 |
| 02-c | 측정 없음 | `–점` 단독. 그래프는 수평 점선만, 마커 없음 | P03 |
| 02-d | 스탯 4종 0 | 4개 타일 모두 "0일 / 0% / 0회 / 0일". 카드 유지 · 카운트업 생략 | P02 |
| 02-e | 뇌 점수 하락 | `77점 → 69점` — 하락도 그대로 표기하며 문구를 바꾸지 않음 | P01 |
| 02-f | 변화 없음 | `69점 → 69점` · 꺾은선 수평 | P01 |
| 02-g | 경계값 | `0점 → 100점` — F-08이 낼 수 있는 최소 · 최대 | P01 |

> 10/30 재검사 입력 전까지는 전원이 **측정 1회** 상태입니다. 이 화면의 기본값은 시작→종료가 아니라 단일 값 하나이며, 그 상태로 완성돼 보여야 합니다.
> 뇌 점수는 SMCQ(0~13 정수) 변환값이라 **0 · 8 · 15 · 23 · 31 · 38 · 46 · 54 · 62 · 69 · 77 · 85 · 92 · 100** 중 하나만 나옵니다. 그 사이 값(68 · 76 등)이 보이면 산식을 타지 않은 값입니다.

### SC-RP-03 집중 리듬

| 케이스 | 조건 | 화면 처리 | P코드 |
|---|---|---|---|
| 0 | 정상 | 화요일 · 오후 1시 · 막대 5개 중 화요일만 강조 | P01 |
| 03-a | 일부 요일 0% | 0% 요일도 **트랙과 "0%"를 그대로 표시**. 막대 5개는 항상 모두 그림 | P02 |
| 03-b | 2개 요일 동률 | 타일 **"골고루"** · 동률 막대 **모두 강조** · 멘트는 "{시간}에 자주 만났어요" | P06 |
| 03-c | 전 요일 동률 | 동일 처리 (동률 개수와 무관하게 표기는 하나) | P06 |
| 03-d | 시간 동점 | **이른 시간**을 채택. "골고루"를 쓰지 않음 | P06 |
| 03-e | 접속 기록 없음 | 타일 2개 "–" · 막대 5행 전부 0% · **어느 요일도 강조하지 않음** · 멘트를 "아직 기록이 없어요"로 교체. 카드는 유지 | P03 · P13 |

막대 폭 · 퍼센트 값 · 강조(막대 색 + 요일 라벨 Bold + % Bold)는 **항상 함께** 바뀝니다.

### SC-RP-04 게임 하이라이트

| 케이스 | 조건 | 화면 처리 | P코드 |
|---|---|---|---|
| 0 | 3종 이상 | 다크 카드 + 타일 2개 + TOP 3 정상 | P01 |
| 04-a | 해본 게임 2종 | TOP 3 **3행 고정** — 3행째는 자리표시(순위 유지 · 썸네일 35% · "게임 기록이 없어요" · 횟수 미노출) | P05 |
| 04-b | 해본 게임 1종 | 2 · 3행이 자리표시 | P05 |
| 04-c | 게임 0종 | 다크 카드 **미노출** · 타일 "0회 / 0종" 유지 · TOP 3 카드 제목 자리를 **"아직 해본 게임이 없어요"** 한 줄로 대체하고 행은 그리지 않음 · **섹션은 유지** | P02 · P05 |
| 04-d | 최다 게임 동점 | 노션 정렬이 앞선 게임을 하이라이트로. TOP 3에는 동점 두 게임이 나란히 표시 | P06 |
| 04-e | 게임명 최장 | 다크 카드 게임명이 한 줄에 들어가는지 폭 검증용. **축약 금지** | P01 |

화면 어디에도 **게임 점수를 쓰지 않습니다**. 하이라이트 · TOP 3 모두 material_code별 완료 횟수 내림차순(F-03b)에서 뽑으며, 하이라이트(S4-03) · 타일(S4-04) · TOP 3 1위(S4-06)는 **항상 같은 게임**입니다.

### SC-RP-04B 활동 점수 랭킹

| 케이스 | 조건 | 화면 처리 | P코드 |
|---|---|---|---|
| 0 | 정상 (12위) | 타일 "12위 / 상위 2%" · 상위권 1–3위 블러 · 구분선 · 내 순위 다크 행 | P01 |
| 04B-a | 내 순위 1위 | **1위 행만 선명한 다크 행**으로 띄우고 2 · 3위는 블러 유지. 아래 구분선과 별도 내 행을 만들지 않음 (같은 사람을 두 번 그리지 않음) | P08 |
| 04B-b | 내 순위 2–3위 | 해당 순위 행만 선명 처리. 나머지는 블러 유지 | P08 |
| 04B-c | 내 순위 4위 | 상위권 3행은 전부 블러, 구분선 아래 내 행 표시 (정상과 동일 구조) | P01 |
| 04B-d | 최하위 823위 | "823명 중 823위" · 상위 100% · **하단 "아래로 N명" 요약 전체 미노출** | P08 |
| 04B-e | 활동 점수 0점 | 공동 최하위를 그대로 표기. 점수 "0" | P08 |
| 04B-f | 동점 공동 순위 | 1 · 2위가 같은 점수. 다음 순위는 인원수만큼 건너뜀 | F-06 |
| 04B-g | 공동 1위 4명 · 본인 포함 | 상위권 3행 전부 "1" · **본인 행이 첫 줄**에 선명하게, 나머지는 참여일 → 가입일 순 2명 · 4번째 동점자는 미노출 · 안내 각주 표시 | F-06 |

- 상위권 1–3위 행은 **행 단위로 blur 5px + 투명도 50%**입니다. 내 행은 이 처리에서 제외하고 다크 배경 + 그림자로 위에 띄웁니다. 블러는 개인정보 보호가 아니라 **"위에 누가 있다"만 전하고 다음 기수 동기를 남기는 연출**이며, 이름은 블러와 무관하게 **마스킹된 값**을 넣습니다.
- 파생 수치 두 개는 모수에서 계산합니다 — 상위 % = `CEIL(내 순위 ÷ 823 × 100)`, 하단 문구 N = `823 − 내 순위`. **타일의 상위 %와 카드 배지의 상위 %는 같은 값**이어야 합니다.
- 상위권과 내 행 사이 구분선은 **문구 없는 전체 폭 가로선 1줄**입니다. 구간 표기("4위 – 822위")를 넣지 않습니다.

### SC-RP-05 마음 기록

| 케이스 | 조건 | 화면 처리 | P코드 |
|---|---|---|---|
| 0 | 주의 → 양호 | `7점 → 3점` · 고스트(7점) + 실선(3점) · 상태 "양호"(#0A6B47) | P01 |
| 05-a | 종료 = 주의 | `12점 → 7점` · 상태 "주의" · 색은 **본문색 #191F28** | P01 |
| 05-b | 종료 = 위험 | `14점 → 12점` · 상태 "위험" · 본문색 · **개선이어도 상담 안내 노출** | P13 |
| 05-c | 경계값 4점 | `5점 → 4점` — 4는 양호, 5는 주의 (경계값 포함 확인) | P01 |
| 05-d | 변화 없음 | `2점 → 2점` · **고스트를 겹쳐 그리지 않고 숨김** | P04 |
| 05-e | 악화 | `1점 → 4점` · 고스트가 바늘 오른쪽 | P01 |
| 05-f | 악화 → 위험 | `8점 → 12점` · 상태 "위험" | P13 |
| 05-g | 측정 1회 | 그 값 하나만 표기 · **실선 바늘만** · 고스트 미노출 | P04 |
| 05-h | 측정 없음 | 다크 카드 "–점" · 아크 배경만 남기고 바늘 · 고스트 · 축 모두 미노출 · 상태 텍스트를 **"검사 기록이 없습니다."**로 교체 · 안내박스는 "검사 기록이 없네요!" 쌍 | P03 |

**어느 경우든 구간 표기(S5-07)는 항상 노출**합니다. 상태 텍스트 값은 **양호 / 주의 / 위험** 세 가지뿐이며 "편안함" 같은 감성 표현은 구간과 매핑되지 않으므로 쓰지 않습니다.

### SC-RP-06 걸음수 · 운동

| 케이스 | 조건 | 화면 처리 | P코드 |
|---|---|---|---|
| 0 | 정상 | 714,200보 · 198일 · 3,607보 · 36% · 부산까지 걸었어요 · 약 144kcal 초코파이 하나 | P01 |
| 06-b | 걸은 날 0일 | 총 0보 · 0일 · 하루 평균 **0보(0으로 나누지 않음)** · 도넛 0% · **거리 · 칼로리 카드 미노출** · 섹션은 유지 | P17 |
| 06-c | 목표 초과 | 하루 평균 12,400보 → **중앙 숫자 124%** · 아크는 100%에서 정지 | P07 |
| 06-d | 거리 N=1 · 편도 | 231,000보 → 150km → 대전 → "대전까지 걸었어요" (회차 미표기) | P01 |
| 06-e | 거리 N=2 · 왕복 | 1,350,000보 → 877km → 부산(420km) → N=2 → "부산까지 **왕복했어요**" | P01 |
| 06-f | 최소 구간 | 12,300보 → 7km → "남산 한 바퀴까지 걸었어요" · 약 62kcal 삶은 달걀 하나 | P01 |
| 06-g | 7자리 총 걸음 | 1,024,300보 — 타일 폭 검증용(180px 배분 · 26px · 자간 −3.5%) | P01 |
| 06-x | 걸음 미연동 | **섹션 전체 미노출.** 리포트가 7개 섹션이 됨 | P16 |

- 하루 평균의 분모는 **걸은 날 수**이며 참여일이 아닙니다.
- 칼로리는 **하루 평균 걸음** 기준이고 거리는 **총 걸음** 기준입니다. 둘을 바꾸면 안 됩니다.
- 헤드라인의 도착지와 각주의 편도 km는 **반드시 같은 랜드마크**입니다.
- 최다 걸음일 타일은 삭제됐습니다. payload `steps.max_day`도 산출하지 않습니다.

### SC-RP-07 수료증

| 케이스 | 조건 | 화면 처리 | P코드 |
|---|---|---|---|
| A | 수료 분기 A | "수 료 증" · "수료를 축하드려요!" · 통계 2개 · 버튼 3개 · 폭죽 | K-06 |
| B | 수료 분기 B | "참 여 기 록" · "이만큼 함께했어요!" · 통계 2개 · 버튼 3개 · 폭죽 | K-06 |
| C | 수료 분기 C | "참 여 확 인" · "4기에도 함께해요!" · **통계 없음** · 버튼 2개 · **폭죽 미실행** | K-06 |
| 07-d | 이름 미설정 (닉네임 · 이름 둘 다 없음) | 둘 다 없을 때만. 증서 이름을 **"회원님"** 단독 표기 ("님" 중복 금지). 있으면 **원문 그대로**(마스킹 없음) | P12 |
| 07-e | 캡처 실패 | 증서 아래 안내 카드 "저장하지 못했습니다 / 잠시 후 다시 시도해 주세요." 재시도 1회 후 노출 | P15 |
| 07-f | 242일 · 100% | 통계 최대값 폭 검증 | P01 |
| 07-g | C분기 본문 최장 | 3줄 본문이 정사각형 안에서 넘치지 않는지 검증 | P01 |
| 07-h | 4기 신청 확인 | 하단 **바텀시트** — "2027년 인천 남동구 보건소 치매예방 프로그램을 신청하시겠습니까?" · [닫기] [네, 신청합니다] | K-13 |
| 07-l | 신청 완료 후 복귀 | "4기도 참여하기" 버튼 **비활성 + "4기 신청이 완료되었습니다"** | P13 |

- 증서는 **정확히 1:1 정사각형**(375px 뷰포트에서 335 × 335)입니다. 인스타 · 카카오톡 프로필로 바로 쓰이므로 내용이 넘쳐 잘리는 일이 없어야 합니다.
- **분기는 서버 `cert.tier`를 그대로 신뢰**하고 클라이언트에서 재판정하지 않습니다.
- 인장은 **mix-blend-mode: multiply** 필수 — normal로 두면 금박 위에서 스티커처럼 뜹니다.
- **이미 신청한 사용자**는 확인 시트를 건너뛰고 완료 화면으로 직행합니다.
- **비로그인**은 로그인 유도가 원칙이나, 리포트 진입 자체가 막혀 있어 실질적으로 도달하지 않는 경로입니다.

### SC-RP-08 4기 신청

| 케이스 | 조건 | 화면 처리 | P코드 |
|---|---|---|---|
| 08-0 | 신청 완료 | 배지 "맬리브레인 4기" · 헤딩 "신청이 완료되었습니다." · 체크 아이콘 · "자세한 문의사항은 {전화번호}로 문의 바랍니다. 감사합니다." · 버튼 [홈으로 돌아가기] | P01 |
| 08-a | 신청 실패 | 배지 "4기 신청" · 헤딩 "신청하지 못했습니다." · 느낌표 아이콘 · "잠시 후 다시 시도해 주세요. 문제가 계속되면 {전화번호}로 문의 바랍니다." · 버튼 [다시 시도] [홈으로 돌아가기] | P18 |

**색만으로 성패를 알리지 않습니다** — 심볼 · 헤딩 · 본문이 함께 바뀝니다.

### 전 화면 · 시스템 상태

| 케이스 | 조건 | 화면 처리 | P코드 |
|---|---|---|---|
| net-loading | payload 도착 전 | 전체 화면 스피너 + "리포트를 불러오는 중이에요". 3초 초과 시 "조금만 기다려주세요" 추가. 스켈레톤 금지 | P19 |
| net-offline | 요청 실패 · 타임아웃 | 전체 화면 안내 "연결이 끊겼어요 / 네트워크 상태를 확인하고 다시 시도해 주세요." + [다시 시도]. 부분 렌더 금지 | P18 |
| gate-batch | payload 미생성 | 진입 차단 + "리포트를 준비하고 있어요" | P09 |
| gate-withdrawn | 탈퇴자 | 진입 차단 + 안내 후 홈 복귀. 배너도 미노출 | P10 |

---

## 4. 예외 화면 장수 요약

| 화면 | 정상 | 예외 케이스 | 소계 |
|---|---|---|---|
| SC-RP-01 | 1 | 01-a · b · c · d | 5 |
| SC-RP-02 | 1 | 02-a ~ g (7) | 8 |
| SC-RP-03 | 1 | 03-a ~ e (5) | 6 |
| SC-RP-04 | 1 | 04-a ~ e (5) | 6 |
| SC-RP-04B | 1 | 04B-a ~ g (7) | 8 |
| SC-RP-05 | 1 | 05-a ~ h (8) | 9 |
| SC-RP-06 | 1 | 06-b · c · d · e · f · g · x (7) | 8 |
| SC-RP-07 | 3 (A · B · C) | 07-d · e · f · g · h · l (6) | 9 |
| SC-RP-08 | 1 (08-0) | 08-a | 2 |
| 전 화면 공통 | — | net-loading · net-offline · gate-batch · gate-withdrawn (4) | 4 |
| **합계** | **11** | **54** | **65** |

---

## 5. 게임 점수 결손 — 이 리포트의 전제

안드로이드 앱 오류로 게임 점수가 서버에 저장되지 않다가 **2026-08-18 강제 업데이트로 정상화**됐습니다. 그 이전 기록은 복구 불가입니다.

| 월 | 게임 완료 | 점수 저장 | 저장률 |
|---|---|---|---|
| 2026-03 | 11,754 | 89 | 0.8% |
| 2026-04 | 11,289 | 22 | 0.2% |
| 2026-05 | 12,013 | 155 | 1.3% |
| 2026-06 | 12,759 | 264 | 2.1% |
| 2026-07 | 12,571 | 191 | 1.5% |
| 8/18 이후 | 2,534 | 2,292 | 90.4% |

표는 2026-08-24 조회 시점 기준입니다. 8/1–8/17 구간은 강제 업데이트 전후가 한 달 안에 섞여 있어 따로 세지 않았고, 9 · 10월은 조회 시점 이후라 아직 없습니다.

**영향 범위** — 기수 3/3–10/30 중 점수가 정상 축적된 구간은 8/18 이후뿐입니다. 기수의 약 70% 구간 점수가 **영구 결손**이며, 823명 중 점수 기록 보유자는 **157명(19.1%)**입니다. 게임을 한 번이라도 완료한 438명 중에서도 점수가 남은 건 157명뿐입니다.

**확정** — 하이라이트 카드는 **완료 횟수 기준**입니다. 완료 횟수는 전 기간 오류 없이 기록돼 438명 전원 산출됩니다. 최고 점수 기준은 채택하지 않습니다 — 점수 보유자가 157명뿐이어서 나머지 666명이 빈 카드를 보게 됩니다. 점수 기반 표기(최고 점수 · 내 점수 TOP 3)는 이번 기수에서 사용하지 않으며, 다음 기수 점수 축적이 정상화된 뒤 복원합니다.

---

## 6. 검수 방법

`index.html`(검수용)을 브라우저로 열면 좌측 패널에서 위 케이스를 전부 전환할 수 있습니다.

- 화면별 드롭다운 9개 + 네트워크 상태 1개 — 선택 즉시 우측 화면이 다시 그려집니다.
- `전부 정상으로` — 모든 화면을 정상 케이스로 되돌립니다.
- `네트워크 상태` — 정상 → 로딩(P19) → 연결 끊김(P18) 순환.
- `▶ 모션 다시 보기` / `모션 끄기` — M-01 ~ M-11 재생 및 reduced-motion 상태 확인.
- 상단 점프 칩(01 · 02 · … · 08)으로 섹션 이동.
- `📋 변경이력` — 최초 기획(2026-07-21)부터 현재 판까지의 변경이력 패널. 문서 7과 같은 내용.
- 수료증 화면의 저장 버튼을 누르면 저장 파일명(`맬리브레인_3기_수료증_{이름}.png`)이 토스트로 표시됩니다.

---

문서 2 / 4 · 예외 상태 명세 · v3.0 · 2026-09-02
함께 보기 — 1. 정책 · 상수 · 산식 · 3. 화면설계서 · 4. IA · 개발 티켓
