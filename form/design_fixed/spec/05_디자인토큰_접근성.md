# 05. 디자인 토큰 · 접근성 기준

디자인 확정본(v0.3) 기준입니다. **토큰은 그대로 쓰고, "지켜야 할 기준"은 유지**해 주세요.

## 확정 토큰 (v0.3 — 클로드디자인 산출본)

리디자인 결과가 정본입니다. 모드별로 강조색이 갈립니다.

```css
/* 강조 — 모드별 */
--accent-user:       #1F3C5D;   /* 어르신 모드 — 헤더, 주요 버튼, 선택 상태, 포커스 */
--accent-user-hover: #16304B;
--accent-staff:      #3A342C;   /* 직원 모드 — 헤더, 주요 버튼 */
--accent-staff-hover:#2B2723;

/* 직원 모드 표식 */
--staff-bar-bg:   #2B2723;  --staff-bar-fg: #F0D6A4;  --staff-bar-line: #A9701A; /* 하단 3px */
--staff-target-bg:#FAF2E3;  --staff-target-line:#D9BE85; --staff-target-fg:#4A340B;

/* 면 */
--app-bg:    #F4F3F0;
--surface:   #FFFFFF;        /* 카드 · 입력 */
--fixed-bg:  #EDEBE5;        /* 고정값 필드 "인천광역시 남동구" */ --fixed-line:#DCD8CF;

/* 글자 */
--ink:   #1A1F26;   /* 본문     14.9:1 */
--ink2:  #59636D;   /* 보조      5.5:1 */
--ink3:  #6B7680;   /* 라벨·placeholder  4.6:1 */

/* 선 */
--line-input:  #D6D2C9;   /* 1.5px — 대비 1.5:1, 아래 경고 참고 */
--line-card:   #E4E1DA;   /* 1px */
--line-strong: #C8C3B8;   /* 2px, 보조 버튼 */
--line-list:   #EFEDE7;

/* 상태 4종 — 배경 / 보더 / 텍스트 / 아이콘 원 / 기호 */
오류 e  #FBEFED / #EBC7C1 / #8B1F19 / #A8241F / !     8.1:1
경고 w  #FAF2E3 / #E6D3AE / #6A4A11 / #8A5412 / !     7.3:1
성공 k  #E9F1EC / #C3D9C9 / #14543E / #17624A / ✓     7.7:1
안내 i  #ECF1F6 / #C6D6E4 / #1A3654 / #1F3C5D / i    10.9:1

/* 필수 배지 */  #FBEFED / #EBC7C1 / #9E241D        6.9:1
/* 완료 체크 원 */ #17624A   /* 관외 경고 원 */ #8A5412

/* 글자 확대 배율 */ --fs: 1 | 1.15 | 1.3 | 1.6
/* 고정 바 실측 높이 */ --ctah  (JS가 측정해서 내림)
```

서체 **Pretendard Variable** · 라운드 입력 14px / 카드 16px / 배지 6px / 시트 상단 28px / 칩 999px
포커스 `outline:3px solid rgba(31,60,93,.3); outline-offset:1px` + 보더색 강조

### 크기
본문 17px · 보조 15px · 라벨 17px/700 · 입력 19px/600 · 제목 24px/800(-.04em) · 결과 화면 제목 27px
주요 버튼 20px/800 · 값 표시 17px/700 · 배지·캡션 13~14px/700
입력·버튼 높이 `clamp(58px, calc(62px*var(--fs)), 88px)` · 체크박스 34px(전체동의) / 32px(항목)
폼 좌우 20px · 섹션 간 28px · 섹션 내 10px

### 인증번호 입력
`letter-spacing:.2em` + `font-variant-numeric:tabular-nums` + `font-weight:700` — 6자리를 끊어 읽게 합니다.

### 대비 검증 결과
텍스트 12쌍 전부 4.5:1 이상 통과 (최저 4.64:1 — 라벨·placeholder `#6B7680`/흰 배경).

> **미해결 — 입력칸 테두리 대비 1.5:1**
> 흰 입력칸(`#FFFFFF`)과 앱 배경(`#F4F3F0`)의 대비가 1.11:1이라 **테두리가 필드를 식별하는 유일한 단서**입니다.
> 그 테두리(`#D6D2C9`)가 1.5:1이라 WCAG 2.2 SC 1.4.11(비텍스트 3:1)에 못 미칩니다.
> 밝은 실외 부스에서 어르신에게 입력칸이 안 보일 수 있습니다.
> 테두리를 3:1 이상(예 `#A8A296`)으로 올리거나 입력칸 배경을 한 단계 더 눌러 주세요.

## 지켜야 할 기준

### 1. 크기 하한

| 항목 | 하한 | 이유 |
|---|---|---|
| 본문 | **17px** | 노안 기준. 일반 서비스의 14~15px은 읽히지 않음 |
| 입력값 | **19px** | 자기가 뭘 쳤는지 확인할 수 있어야 오타를 스스로 잡음 |
| 터치 타겟 | **44px**, 주요 버튼 56~64px | 손떨림·건조한 손끝에서의 인식률 |
| 체크박스 | **34px** + 행 전체 터치 | WCAG 최소치(24px)로는 부족 |

### 2. 대비

- 본문 **4.5:1** 이상, 큰 글씨 **3:1** 이상 (WCAG 2.2 AA)
- **현장이 밝은 실외**(부스·복지관)일 수 있음. 저대비 회색 글씨 금지
- `--ink3`(#8B95A1)가 흰 배경에서 대비 하한. 그보다 옅은 회색은 쓰지 않음

### 3. 글자 확대 정책 ★ 가장 자주 깨지는 부분

어르신은 **폰 자체 글자 크기를 이미 키워둔 상태**로 들어옵니다.
전부 같은 비율로 키우면 헤더만 한 화면을 다 먹고 첫 입력칸이 화면 밖으로 밀려납니다.

| 구분 | 대상 | 처리 |
|---|---|---|
| **크게 늘림** | 라벨 · 입력값 · 버튼 · 오류 메시지 | 배율을 거의 그대로 반영 |
| **상한을 검** | 로고 · 제목 · 부제 · 도움말 · 캡션 | `clamp()`로 최대치 고정 |

```css
/* 예시 */
.org h2 { font-size: clamp(21px, calc(22px * var(--fs)), 27px); }   /* 제목 — 상한 */
.help   { font-size: clamp(13px, calc(13px * var(--fs)), 17px); }   /* 도움말 — 상한 */
input   { font-size: clamp(19px, calc(19px * var(--fs)), 26px); }   /* 입력값 — 크게 */
.btn    { font-size: clamp(19px, calc(20px * var(--fs)), 27px);
          min-height: clamp(58px, calc(64px * var(--fs)), 88px); }  /* 버튼 — 상한 88px */
```

검증: **360×640 화면 + 글자 200%** 에서
- 가로 스크롤이 생기지 않을 것
- 첫 입력칸이 첫 화면에 보일 것
- 고정 버튼이 화면 높이의 **30%를 넘지 않을** 것

### 4. 고정 버튼 (sticky)

```css
.cta {
  position: sticky;
  bottom: 0;
  z-index: 26;
  padding: 12px 16px calc(14px + env(safe-area-inset-bottom));
  background: rgba(247,249,250,.97);
  backdrop-filter: blur(10px);
  border-top: 1px solid var(--line);
  box-shadow: 0 -6px 20px rgba(18,32,26,.10);
}
@media (max-height: 560px) { /* 가로 모드·확대 시 여백 축소 */ }
```

- **조상 요소에 `overflow:hidden`을 쓰지 마세요.** sticky가 조용히 죽습니다.
- 입력칸에 포커스가 가면 고정 버튼 높이를 계산해, 가려질 경우 화면 가운데로 끌어옵니다.
- 아직 못 본 내용이 있으면 버튼 위에 `아래에 더 있어요 ▾` 알약을 띄우고, 한 번 내리면 감춥니다.

### 5. 상태 표현 — 색 + 아이콘 + 문구 3중

```
성공  ✓  초록 배경 + 초록 테두리 + 진한 초록 글씨
오류  !  연빨강 배경 + 빨강 테두리 + 진한 빨강 글씨
경고  !  연주황 배경 + 주황 테두리 + 진한 주황 글씨
```
- 색만으로 구분하지 않습니다. **흑백으로 봤을 때도 구분**돼야 합니다.
- 상태 메시지는 해당 필드 **바로 아래**. 화면 상단 요약 배너는 쓰지 않습니다.

### 6. 체크박스

- 미체크 = **빈 네모** (회색 체크 표시 금지 — 이미 체크된 것처럼 보임)
- 체크 = 브랜드 색 바탕 + 흰 체크
- 행 전체가 터치 영역

### 7. 접근성 마크업

```html
<!-- 상태 메시지 -->
<p role="status" aria-live="polite">…</p>

<!-- 체크박스 -->
<button role="checkbox" aria-checked="false" aria-labelledby="c1t">…</button>

<!-- 시트 -->
<div role="dialog" aria-modal="true" aria-labelledby="…">…</div>
<!-- 열 때 첫 조작 대상으로 포커스, 닫을 때 연 버튼으로 복귀, ESC·뒤로가기 지원 -->

<!-- 입력 -->
<input type="tel" inputmode="numeric" autocomplete="tel">
<input type="text" inputmode="numeric" autocomplete="one-time-code">
<input type="text" autocomplete="name">
```
- **비활성(disabled) 버튼을 쓰지 않습니다** — 스크린리더가 읽지 않고 아무 피드백도 주지 않습니다.
- 포커스 링은 반드시 보이게: `outline: 3px solid rgba(brand,.32); outline-offset: 1px`
- `@media (prefers-reduced-motion: reduce)` 에서 애니메이션을 끕니다.
- 숫자 카운트다운은 `font-variant-numeric: tabular-nums` (자릿수 흔들림 방지)

### 8. 모션

- 스크롤 이동은 `behavior:"smooth"`, 그 외 전환은 거의 쓰지 않습니다
- **화면이 저절로 바뀌는 연출 금지** — 시니어는 오작동으로 인식합니다
- 스피너 외의 로딩 연출(스켈레톤 등)은 쓰지 않습니다 — 내용이 있는 것으로 오해합니다
